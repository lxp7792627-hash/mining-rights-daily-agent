# 架构和维护

```text
src/
  core/          共享状态、UTC 日期、网络边界
  providers/     新闻、PDF、行情逻辑，与 MCP 传输解耦
  servers/       三种独立服务入口与 Zod 输入约束
  agent/         MCP Client 生命周期、编排、报告、CLI
test/            领域单元测试和真实 stdio 集成测试
scripts/         本地 MCP 配置生成器
```

## 生命周期

Agent 先解析用户输入，启动三个子进程并建立 MCP 会话；新闻查询、PDF 提取、当日行情和趋势同时调用。新闻返回后补取最多五篇正文。每个来源调用独立捕获错误，生成报告时逐项展示结果状态；`finally` 关闭全部客户端，避免异常路径遗留服务器。

工具输入使用 Zod 校验，MCP 服务 stdout 只用于协议。客户端校验返回的 MCP 外层结构和领域结果状态，领域 data 类型由仓库内部可信服务保证；如改为第三方 MCP Server，应增加完整输出 schema 校验。

## 安全和资源限制

- 无模型执行或 shell 工具暴露给外部内容，不执行网页/PDF 内的指令。
- 下载只允许公开 HTTPS，无 URL 凭据；限制重定向、下载量与超时；可设置精确域名白名单。
- PDF 最大 20 MB、300 页；HTML/RSS 最大 2 MB；新闻结果最多 20 条，正文获取最多 5 篇。
- 正文进入 Markdown 前转义，引用链接拒绝非 HTTPS 协议。
- Docker 使用非 root、只读根文件系统、丢弃 Linux capabilities；输出目录和临时目录单独提供写权限。
- 凭据不进入仓库；行情文件与本地配置忽略提交，环境变量按白名单传给子进程。

该程序面向本机可信调用方，不提供公网 HTTP 服务。若部署为多人服务，应增加认证、限流、独立 PDF 工作进程的 CPU/内存限制和网络 egress 防火墙。当前 PDF 文本解析的 300 页限制不能代替操作系统资源隔离。

## 扩展点

增加行情供应者应保持 Price schema，先在 Provider 层做供应商字段映射、授权和基准校验。增加 PDF 版式应以真实脱敏样本补充 parser 和失败测试，保留页码与原始证据，不扩大无依据猜测范围。

如需自然语言生成摘要，可在 collect 之后添加模型模块：只接受已校验的证据、要求结构化引用、再次校验引用归属；不能让生成模型覆盖缺失或 needs_review 状态。当前确定性实现不依赖任何模型服务。
